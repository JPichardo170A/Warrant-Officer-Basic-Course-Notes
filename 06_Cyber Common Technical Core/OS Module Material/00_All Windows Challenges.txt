===============================
 WINDOWS AUDITING & LOGGING Q&A
===============================

1. What Sysinternals tool will allow you to read the SQLite3 database containing the web history of Chrome?
   -> Step 1: Sysinternals provides strings.exe to extract readable text from binaries and databases.
   -> Answer: strings.exe

2. What is the registry location of recent docs for the current user?
   -> Step 1: Open regedit or use PowerShell `Get-ItemProperty`.
   -> Command: reg query "HKCU\Software\Microsoft\Windows\CurrentVersion\Explorer\RecentDocs"
   -> Answer: HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Explorer\RecentDocs

3. BAM settings are stored in different registry locations based on the version of Windows 10.  
   What version of Windows 10 is workstation2 running? (4-digit release number)
   -> Step 1: Run `winver` or query registry:  
              reg query "HKLM\SOFTWARE\Microsoft\Windows NT\CurrentVersion" /v ReleaseId
   -> Answer: 1803

4. Figure out the last access time of the hosts file. (Flag format: mm/dd/yyyy)
   -> Step 1: Open PowerShell
   -> Step 2: Run `(Get-Item "C:\Windows\System32\drivers\etc\hosts").LastAccessTime`
   -> Flag: Thursday, August 14, 2025 9:22:41 PM

5. What is the literal path of the prefetch directory?
   -> Step 1: Default system path is constant across Windows versions.
   -> Answer: C:\Windows\Prefetch

6. In the Recycle Bin, what are the first two characters of the file containing actual contents of recycled file?
   -> Step 1: Navigate to `$Recycle.Bin` folder
   -> Step 2: Look for files starting with `$R`
   -> Answer: $R

7. In the Recycle Bin, what are the first two characters of the file containing original filename, path, file size, and deletion time?
   -> Step 1: Same `$Recycle.Bin` folder
   -> Step 2: Look for files starting with `$I`
   -> Answer: $I

8. What are the first 8 characters of the GUID used in the UserAssist registry key (Windows 7 and later)?
   -> Step 1: Registry location:  
              HKCU\Software\Microsoft\Windows\CurrentVersion\Explorer\UserAssist
   -> Step 2: Note the GUID key.  
   -> Answer: CEBFF5CD

9. What cipher method are UserAssist files encoded in?
   -> Step 1: UserAssist values are ROT13 encoded.
   -> Step 2: Apply ROT13 to decode. Example: `tr 'A-Za-z' 'N-ZA-Mn-za-m'`
   -> Answer: ROT13 (Rotate by 13)

10. What main Windows log would show invalid login attempts?
    -> Step 1: Open Event Viewer → Windows Logs → Security
    -> Step 2: Filter on Event ID 4625 (failed logon)
    -> Answer: Security log

11. What main Windows log shows whether Windows updates were applied recently?
    -> Step 1: Open Event Viewer → Windows Logs → System
    -> Step 2: Filter on Event IDs 19, 20, 21 (Windows Update)
    -> Answer: System log

12. When reading logs, truncated lines end with "...".  
    What format-table switch/argument displays the full output? (Flag format: -argument)
    -> Step 1: Use PowerShell Format-Table with `-Wrap`
    -> Example: Get-EventLog -LogName System | Format-Table -Wrap
    -> Answer: -wrap

13. Find the questionable website that a user browsed to (using Chrome), that appears to be malicious.  
    Machine: Workstation2 (ssh from Admin_Station)
    -> Step 1: Extract Chrome history DB:  
               C:\Users\<User>\AppData\Local\Google\Chrome\User Data\Default\History
    -> Step 2: Use strings.exe or sqlite3 to query `urls` table.
    -> Step 3: Identify suspicious domain.  
    -> Flag: [TODO]

14. There is a file that was recently opened that may contain PII. Get the flag from the contents of the file.  
    Hint: Not interested in numbers.
    -> Step 1: Check RecentDocs registry key or JumpLists for recently opened files.
    -> Step 2: Open candidate file and extract contents.
    -> Flag: [TODO]

15. Enter the full path of the program that was run on this computer from an abnormal location.
    -> Step 1: Check Prefetch folder for unusual EXE names.  
    -> Step 2: Correlate with parent folder (abnormal location = outside Program Files/Windows).
    -> Flag: [TODO]

16. What is the creation time of the questionable file in the prefetch folder? (Flag format: mm/dd/yyyy)
    -> Step 1: Open PowerShell
    -> Step 2: Run `(Get-Item "C:\Windows\Prefetch\DARK_FORCES-8F2869FC.pf").CreationTime.ToString("MM/dd/yyyy")`
    -> Flag: 02/23/2022

17. Enter the name of the questionable file in the prefetch folder.
    -> Step 1: Check directory listing of C:\Windows\Prefetch
    -> Answer: DARK_FORCES-8F2869FC.pf


===============================
 01_WINDOWS_POWERSHELL
===============================

1. Windows_PowerShell_Alias:
   -> 5

2. What PowerShell command lists all of the contents of a directory?
   -> Get-ChildItem  
   -> (It can also be Dir or ls since the question is vague.)

3. How to get the count of Get-Process properties in value:
   -> (Get-Process | Select-Object -First 1 | Get-Member -MemberType Property).Count

4. How to get the count of words in a file:
   -> (Get-Content .\words2.txt).Count

5. How to get the count for files inside the Videos (or any) folder:
   -> (Get-ChildItem "C:\Users\CTF\Videos").Count

6. Compare files and output the difference:
   -> Compare-Object (Get-Content new.txt) (Get-Content old.txt)

7. Sort and find specific line from a file:
   -> (Get-Content .\words.txt | Sort-Object -Descending)[20]

8. Count the number of unique words in words.txt:
   -> (Get-Content words.txt | Sort-Object -Unique | Measure-Object).Count 

9. How many methods are available for the Get-Process cmdlet?  
   Machine: Admin_Station
   -> (Get-Process | Get-Member -MemberType Method).Count

10. Count the number of times, case-insensitive, "gaab" is listed in words.txt  
    Note: File Location - C:\Users\CTF\Desktop\CTF
   -> (Select-String -Path words.txt -Pattern "gaab").Count

11. Count the number of words, case-insensitive, with either "a" or "z" in a word, in words.txt:
   -> (Select-String -Path words.txt -Pattern "[az]").Count


12. Use a PowerShell loop to unzip the Omega file 1,000 times and read what is inside.  
    Note: Make sure you back up the .zip file to a different directory before attempting this challenge.  
    -> Unzips each layer into its own folder, stops when no more zip files are found.

    -> Step 1: Setup
-------------------------------------------------
$startZip = "Omega1000.zip"
$workingDir = "Omega1000.Unzipped"
$layer = 1

# Create working directory
if (-not (Test-Path $workingDir)) {
    New-Item -ItemType Directory -Path $workingDir | Out-Null
}

# Copy initial zip
Copy-Item $startZip "$workingDir\Layer_$layer.zip"

# Loop through nested zips
while ($true) {
    $zipPath = "$workingDir\Layer_$layer.zip"
    $extractPath = "$workingDir\Layer_$layer"

    Expand-Archive -Path $zipPath -DestinationPath $extractPath -Force
    Write-Host "📂 Unzipped Layer $layer"

    # Look for next zip
    $nextZip = Get-ChildItem -Path $extractPath -Filter *.zip -Recurse | Select-Object -First 1

    if ($nextZip) {
        $layer++
        Copy-Item $nextZip.FullName "$workingDir\Layer_$layer.zip"
    } else {
        Write-Host "✅ Final layer reached at Layer $layer"
        break
    }
}

# Read contents of final layer
$finalFiles = Get-ChildItem -Path $extractPath -File -Recurse

foreach ($file in $finalFiles) {
    Write-Host "`n📖 Reading file: $($file.Name)"
    Get-Content $file.FullName
}
-------------------------------------------------

    -> Step 2: Answer is Kung-Fu


13. Count the number of words in words.txt that meet the following criteria:  
    - "a" appears at least twice consecutively  
    - followed immediately by any of the letters a through g  
    Example: aac... aaa...  
    Note: File Location - C:\Users\CTF\Desktop\CTF
   -> (Select-String -Path words.txt -Pattern "a{2,}[a-g]").Count

14. What command would tell you if there was a profile loaded for All Users All Hosts?  
    (Flag is the full command syntax)
   -> Test-Path $PROFILE.AllUsersAllHosts  
   -> Challenge only allows ONE attempt.

15. Malware is running on a PowerShell profile on the File-Server.  
    Based on PowerShell profile order of precedence (what is read first), find the correct flag.  
    The flag is the string after the #, without the preceding space.
   -> Step 1: $Profile  
   -> Step 2: Get-Content $Profile --->>> output of this should be your flag.

16. To find a user by using SID:
   -> Get-LocalUser | Select Name, SID

17. Script to connect to file server:
   -> New-PSDrive -Name FileServer -PSProvider FileSystem -Root "\\file-server\warrior share"

18. Why do we care?
   -> Persistence: Techniques adversaries use to keep access to systems across restarts, changed credentials, and other interruptions that could cut off their access.  
   -> Privilege Escalation: Techniques adversaries use to gain higher-level permissions on a system or network.


===============================
 REGISTRY - CAPTURE THE FLAGS
===============================

1. What Windows registry path is the Volatile Hive?
   -> HKLM\HARDWARE

2. What registry key creates the Wow6432Node to represent 32-bit applications that run on a 64-bit version of Windows?
   -> HKEY_LOCAL_MACHINE\SOFTWARE

3. In what registry path are the BOOT_START drivers located?
   -> HKLM\SYSTEM\CurrentControlSet\Services

4. What start value do BOOT_START drivers have in the registry?
   -> 0x0

5. During kernel initialization, what registry location is read containing all SYSTEM_START drivers?
   -> HKLM\SYSTEM\CurrentControlSet\Services

6. SERVICE_AUTO_START drivers and services are loaded after kernel initialization.  
   What start value do they have in the registry?
   -> 0x2

7. What start value do SERVICE_DEMAND_START drivers and services have in the registry?
   -> 0x3

8. When accessing a remote registry which are the only 2 accessible HKEYs?
   -> HKLM and HKU

9. What PowerShell cmdlet will list currently mapped drives?
   -> Get-PSDrive

10. What is the native Windows GUI tool for managing the registry?
    -> Regedit

11. What registry hive contains all machine settings?
    -> HKEY_LOCAL_MACHINE (HKLM)

12. What registry hive contains all user settings?
    -> HKEY_USERS (HKU)

13. What registry hive contains only the currently logged-in user's settings?
    -> HKEY_CURRENT_USERwhoami

14. The HKEY_CURRENT_USER registry hive is a symbolic link to another registry subkey.  
    What is the subkey that it is linked to?
    -> Step 1: whoami \user to get the SID  
    -> Step 2: reg query "HKEY_USERS\S-1-5-21-2881336348-3190591231-4063445930-1004" to verify the SID.

15. What PowerShell command will list all the subkeys and contents in the current directory and/or will list all the subkeys and the contents of a directory you specify?
    -> Get-ChildItem

16. What PowerShell command will list only the contents of a registry key or subkey?
    -> Get-Item

17. What registry subkey runs every time the machine reboots?  
    Flag format: FULL\PATH\ALL\CAPS
    -> HKLM\SOFTWARE\MICROSOFT\WINDOWS\CURRENTVERSION\RUN

18. What registry subkey runs every time a user logs on?  
    Flag format: FULL\PATH\ALL\CAPS
    -> HKCU\SOFTWARE\MICROSOFT\WINDOWS\CURRENTVERSION\RUN

19. What registry subkey runs a single time, then deletes its value once the machine reboots?  
    Flag format: FULL\PATH\ALL\CAPS
    -> HKLM\SOFTWARE\MICROSOFT\WINDOWS\CURRENTVERSION\RUNONCE

20. What registry subkey runs a single time, then deletes its value when a user logs on?  
    Flag format: FULL\PATH\ALL\CAPS
    -> HKCU\SOFTWARE\MICROSOFT\WINDOWS\CURRENTVERSION\RUNONCE

21. What is the suspicious value inside of the registry subkey from your previous challenge named registry_basics_7?
    -> Step 1: reg query HKLM\SOFTWARE\Microsoft\Windows\CurrentVersion\Run  
    -> Step 2: Answer is C:\malware.exe

22. What is the suspicious value inside of the registry subkey that loads every time the "Student" user logs on?
    -> Step 1: get-localuser | select name, sid  
    -> Step 2: reg query HKEY_USERS\S-1-5-21-2881336348-3190591231-4063445930-1003\Software\Microsoft\Windows\CurrentVersion\Run

23. What is the value inside of the registry subkey from registry_basics_9?
    -> Step 1: reg query "HKLM\SOFTWARE\MICROSOFT\WINDOWS\CURRENTVERSION\RUNONCE".ToUpper()  
    -> Step 2: Answer is C:\Virus.exe

24. What is the value inside of the registry subkey that loads a single time when the "Student" user logs on?
    -> Step 1: reg query HKEY_USERS\S-1-5-21-2881336348-3190591231-4063445930-1003\Software\Microsoft\Windows\CurrentVersion\RunOnce  
    -> Step 2: Answer C:\worm.exe

25. Figure out the manufacturer’s name of the only USB drive that was plugged into this machine.
    -> Step 1: reg query "HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Enum\USBSTOR"  
    -> Step 2: Output example:  
       HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Enum\USBSTOR  
       SanDisk9834    REG_SZ    bad  
    -> Step 3: Answer is SanDisk

26. What suspicious user profile, found in the registry, has connected to this machine?
    -> Step 1: Run this script:
-------------------------------------------------
Get-ChildItem "HKLM:\SOFTWARE\Microsoft\Windows NT\CurrentVersion\ProfileList" |
ForEach-Object {
    $sid = $_.PSChildName
    $profilePath = (Get-ItemProperty $_.PSPath).ProfileImagePath
    [PSCustomObject]@{
        SID = $sid
        ProfilePath = $profilePath
    }
}
-------------------------------------------------
    -> Step 2: Profile path list scan and find suspicious profile.  
    -> Answer: Hacker_McHackerson

27. What suspicious wireless network, found in the registry, has this system connected to?
    -> Step 1: reg query "HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows NT\CurrentVersion\NetworkList\Profiles"  
    -> Step 2: Once step 1 is done, run this script:
-------------------------------------------------
Get-ChildItem "HKLM:\SOFTWARE\Microsoft\Windows NT\CurrentVersion\NetworkList\Profiles" |
ForEach-Object {
    $profile = Get-ItemProperty $_.PSPath
    [PSCustomObject]@{
        GUID = $_.PSChildName
        SSID = $profile.ProfileName
        LastConnected = $profile.DateLastConnected
    }
}
-------------------------------------------------
    -> Step 3: Answer based on SSID name: Terror_cafe_network

===============================
 WINDOWS BOOT INIT - Q&A
===============================

1. What is the smallest addressable unit on a hard disk?
   -> Sector

2. What is the first process to spawn on Windows systems after the kernel loads?
   -> System

3. What is the Process ID (PID) of the first Windows process?
   -> 4

4. What is the second boot process to spawn, that then spawns csrss in both session 0 and session 1?
   -> smss.exe

5. What session ID do Windows services operate in?
   -> 0 

6. What process creates access tokens?
   -> lsass.exe

7. What is the parent process to all svchosts?
   -> services.exe

8. What process is waiting with high priority for the Secure Attention Sequence (SAS)?
   -> winlogon.exe

9. What user space process spawns explorer, then dies?
   -> userinit.exe

10. What is the name of the bootloader, with extension, we are using on all of the Windows machines in this environment?
    -> winload.exe

11. Challenge only allows ONE attempt.  
    Based on the boot loader from Init_9, which firmware are we using (BIOS or UEFI) in our environment?
    -> BIOS

12. What file saves the memory state to the hard drive when going into hibernation?
    -> hiberfil.sys

13. What bootloader is responsible for restoring the system to its original state after hibernation?
    -> winresume.exe

14. The system is booting into safe mode.  
    Identify the flag from the command-line output of the command used to diagnose boot issues.
    -> Windows Boot loader : Identifier (number) Description: (Flag 1RF5Zgf9P)

15. The system is booting into safe mode. Correct that, and reboot into the desktop.  
    The flag is on the desktop.

    -> Step 1: Navigate to the QEMU directory where the VM is stored:
       cd "Program Files"
       cd qemu
       (Adjust path based on where the challenge VDI was downloaded and extracted.)

    -> Step 2: Launch the VM using QEMU with the appropriate disk image:
       qemu-system-x86_64 -hda path\to\challenge.vdi -m 2048

    -> Step 3: Inside the VM, open Command Prompt and remove Safe Mode flags using the full boot entry identifier:
       bcdedit /deletevalue {349aadec-dale-11e6-99b2-c4612eddd5ae} safeboot
       bcdedit /deletevalue {349aadec-dale-11e6-99b2-c4612eddd5ae} safebootalternateshell

    -> Step 4: Reboot the VM to apply changes and boot into normal desktop mode:
       shutdown /r /t 0

    -> Step 5: Confirm full desktop access.  
       The flag located on the desktop is: 76Drp6hB


====================================
 WINDOWS ALTERNATE DATA STREAM - Q&A
====================================

1. The ____ determines how the data is stored on disk.
   -> Master File table (MFT)

2. What are NTFS partition sectors grouped into?
   -> cluster or clusters

3. What contains the metadata about all of the files and directories on a NTFS partition?
   -> master file table (MFT)

4. NTFS files are collections of what?
   -> Attributes

5. Which NTFS attribute would store an alternate data stream?
   -> $DATA

6. Which NTFS attribute holds information about a file's encrypted attributes?
   -> $LOGGED_UTILITY_STREAM

7. Which NTFS attribute that is composed of the file security and access control properties?
   -> $SECURITY_DESCRIPTOR

8. In NTFS, what is the type id, in hex, of the attribute that actually stores a NTFS file’s contents?
   -> 0x80 ($DATA)

9. In NTFS what is the maximum number of bytes a MFT entry (containing the entirety of a file) can contain to be considered "Resident Data"?
   -> 1 KB (1024 bytes) by default (can be 2 KB or 4 KB depending on volume format)

10. NTFS permissions can be assigned to a filesystem object in two ways.  
    Which way is intentionally assigned on the file or folder?
    -> Explicit permissions

11. NTFS permissions can be assigned to a filesystem object in two ways.  
    Which way is the result of being assigned permissions as the child of another object?
    -> Inherited

12. Which NTFS file level permission is missing from the list?  
    Write, Read & Execute, Modify, Full Control
    -> Read

13. Which NTFS folder level permission is missing from the list?  
    Read, Write, Read & Execute, Modify, Full Control
    -> List Folder Contents

14. Which NTFS file level permission permits changing the contents of a file, deleting the file but not changing the permissions?
    -> Modify

15. Which NTFS folder level permission allows changing permissions?
    -> Full Control

16. Which NTFS attribute stores the file times of an object?
    -> $STANDARD_INFORMATION

17. What CLI command will only show the letters of attached drives? (3 words)
    -> fsutil fsinfo drives

18. Every file on a Windows system has attributes.  
    What does the "d" attribute mean?
    -> Directory

19. Every file on a Windows system has attributes.  
    What does the "h" attribute mean?
    -> Hidden

20. What PowerShell command will list all files in the current directory, regardless of their attributes?
    -> Get-ChildItem -Force (aliases: gci, ls)

21. What PowerShell command will give you the sha512 hash of a file?
    -> Get-FileHash -Algorithm SHA512

22. What PowerShell command will list permissions of a file?
    -> Get-Acl

23. What Windows file maps hostnames to IP addresses?
    -> hosts

24. Which group has Read and Execute (RX) permissions to the file listed in File_System_Basics_6?
    -> BuiltIN\Users

25. Find the last five characters of the MD5 hash of the hosts file.
    -> Step 1: Get-FileHash -Path "C:\Windows\System32\drivers\etc\hosts" -Algorithm MD5  
    -> Step 2: (Get-FileHash "C:\Windows\System32\drivers\etc\hosts" -Algorithm MD5).Hash[-5..-1] -join ''  
    -> Step 3: Answer is 7566D

26. Examine the readme file somewhere in the CTF user’s home directory.
    -> Step 1: Get-ChildItem -Path "C:\users\ctf" -Recurse -File -Filter "*README*"  
             (found in Favorites folder)  
    -> Step 2: Get-Content "C:\users\ctf\Favorites\README"  
    -> Answer: 123456

27. There is a hidden directory in the CTF user's home directory. The directory contains a file. Read the file.
    -> Step 1: Get-ChildItem -Path "C:\users\ctf" -Directory -Force  
               OR  
               Get-ChildItem -Path "C:\users\ctf" -Recurse -Directory -Force | Where-Object { $_.Attributes -match "Hidden" }  
    -> Step 2: Directory found: "secretsauce" → cd into that directory  
    -> Step 3: Find file "saucey" → cat the file to read contents  
    -> Step 4: Answer is ketchup

28. Find a file in a directory on the desktop with spaces in it. FLAG is the contents of the file.
    -> Step 1: cd into user Desktop → ls  
    -> Step 2: Run script to recurse directories with spaces:
-------------------------------------------------
Get-ChildItem -Path "C:\users\ctf\desktop" -Directory | Where-Object { $_.Name -match " " } |
ForEach-Object { Get-ChildItem -Path $_.FullName -Recurse -File }
-------------------------------------------------
    -> Step 3: Found spaces.txt → read content:
-------------------------------------------------
Get-ChildItem "C:\users\ctf\desktop" -Recurse -File | Where-Object { $_.DirectoryName -match " " } | Get-Content
-------------------------------------------------
    -> Step 4: Answer: 987654321

29. Find the Alternate Data Stream (ADS) in the CTF user's home, and read it.
    -> Step 1: Set-Location "C:\users\ctf"  
    -> Step 2: Get-ChildItem -Recurse -Force -File  
    -> Step 3: List ADS:
-------------------------------------------------
Get-ChildItem -Path "C:\users\ctf\Documents" -Force -File | ForEach-Object {
    Get-Item -LiteralPath $_.FullName -Stream * -ErrorAction SilentlyContinue
}
-------------------------------------------------
    -> Step 4: Filter files with ADS:
-------------------------------------------------
Get-ChildItem -Path "C:\users\ctf\Documents" -Force -File | ForEach-Object {
    $streams = Get-Item -LiteralPath $_.FullName -Stream * -ErrorAction SilentlyContinue
    if ($streams.Count -gt 1) { $streams }
}
-------------------------------------------------
    -> Step 5: Read ADS contents:
       Get-Content -LiteralPath "C:\users\ctf\Documents\nothing_here" -Stream "hidden"  
    -> Step 6: Answer: P455W0RD

30. "Fortune cookies" have been left around the system… Find the hidden password.
    -> Step 1: Navigate to the challenge directory: cd "C:\Users\ctf\Documents"  
    -> Step 2: List files w/ metadata:
       Get-ChildItem -Force | Format-List Name,Length,Mode,LastWriteTime  

    Observation:
       - Multiple empty files (decoys)  
       - One hidden file: "fortune cookie.txt" (168 bytes)  

    -> Step 3: Read "fortune cookie.txt":  
       Get-Content -Path "C:\Users\ctf\Documents\fortune cookie.txt" -Encoding Unicode  

    Output:  
       "only listen to The Fortune Cookie, and disregard all other fortune telling units."  

    -> Step 4: Search system-wide for "fortune":  
       Get-ChildItem "C:\" -Recurse -Force -ErrorAction SilentlyContinue | Where-Object { $_.Name -like "*fortune*" }  

    Findings:  
       - C:\Windows\PLA\not_anihc\The Fortune Cookie (136 bytes)  
       - C:\Windows\PLA\not_anihc\Fortune Cookie Crumb (80 bytes)  
       - Other hidden decoys in System32 and SysWOW64  

    -> Step 5: Read "The Fortune Cookie":  
       Get-Content "C:\Windows\PLA\not_anihc\The Fortune Cookie"  

    Output:  
       "The fortune you seek is inside The Fortune Cookie on this system."  

    -> Step 6: Inspect ADS:  
       Get-Item -LiteralPath "C:\Windows\PLA\not_anihc\The Fortune Cookie" -Stream *  

    Findings:  
       - Default stream :$DATA (136 bytes)  
       - Hidden stream "none" (26 bytes)  

    -> Step 7: Read hidden stream "none":  
       Get-Content -Path "C:\Windows\PLA\not_anihc\The Fortune Cookie" -Stream "none"  

    Output:  
       Password: fortune_cookie  

    Conclusion:  
       Password hidden in ADS of "The Fortune Cookie".  
       Decoys were planted elsewhere.  

    -> Flag/Password: fortune_cookie

31. There are plenty of phish in the C:\Users\CTF, but sometimes they're hidden in plain sight. Find the phish.
    -> Step 1: Check for long lines in words.txt:
       Get-Content ".\words.txt" | Where-Object { $_.Length -gt 20 }  
       Get-Content ".\words.txt" | Where-Object { $_.Length -gt 50 }  

    -> Step 2: Search for non-alphabetic characters:
       Get-Content ".\words.txt" | Where-Object { $_ -match "[^a-zA-Z]" }  
       Get-Content ".\words2.txt" | Where-Object { $_ -match "[^a-zA-Z]" }  

    -> Step 3: Search for HTML/phishing tags:
       Get-ChildItem -Recurse -File | Select-String -Pattern "<html|<form|<input|<script|action="  

    -> Step 4: List files by size:
       Get-ChildItem -Recurse -File | Sort-Object Length -Descending | Select-Object Name, Length  

    -> Step 5: Inspect specific lines in words.txt:
       (Get-Content ".\words.txt")[1336]  
       (Get-Content ".\words.txt")[69]  
       (Get-Content ".\words.txt")[404]  
       (Get-Content ".\words.txt")[9001]  

    -> Step 6: Explore folders on Desktop:
       cd "..\1    -    99" ; Get-ChildItem -Force  
       cd "..\100     -     199" ; Get-ChildItem -Force  
       cd "..\a     -      z" ; Get-ChildItem -Force  

    -> Step 7: Sweep all files in CTF folder:
       Get-ChildItem "C:\Users\CTF" -Recurse -Force -File  

    -> Step 8: Inspect password.txt:
       Get-Content "C:\Users\ctf\documents\password.txt" -TotalCount 20  
       Get-Content "C:\Users\ctf\documents\password.txt" | Where-Object { $_ -ne " I, CTCC Student, will not try to take the easy way out again ." }  

    -> Step 9: Explore WWW folder:
       cd "C:\Users\ctf\documents\WWW"  
       Get-ChildItem -Force  

    -> Step 10: Reveal hidden files with content:
       Get-ChildItem "C:\Users\ctf\documents\WWW" -Force | Where-Object { $_.Length -gt 0 }  

    -> Step 11: Read the phishy file:
       Get-Content "C:\Users\ctf\documents\WWW\200"  

       Flag: phi5hy


===============================
 09_WINDOWS PROCESSES VALIDITY
===============================

1. What is the full path to folder used when Windows redirects 32 bit applications running on a 64bit system?
    -> C:\Windows\SysWOW64

2. What Windows System Service starts the kernel and user mode subsystems?
    -> smss.exe

3. What Windows system process: Runs in session 0, enforces security policy, performs logon functions, handles password changes, creates access tokens, writes to the Security Log?
    -> Local Security Authority Subsystem Service (LSASS)

4. Which is spoolsv.exe?
    -> Server-mode service

5. Which service type is explorer.exe?
    -> User-mode service

6. During a reconnaissance mission you enumerated a host running the dns.exe service. Is this a user pc or a server?
    -> Server

7. A host running firefox and office 365 is most likely what type of host? Server or Client
    -> Client

8. How does a User-Mode Service request resources?
    -> System calls

9. Passively copying currently running processes for comparison later is known as?
    -> Baseline

10. What can execute any part of a process’s code, including parts already being executed?
    -> Thread - a basic unit to which OS allocates processor time.

11. Windows has how many process priority levels?
    -> 32 (0-31)

12. During a network survey you observed a host running inetinfo.exe service. What type of server might you have found?
    -> Internet Information Service (IIS)

13. What Sysinternals tool shows malware persistence locations in tabs within its GUI?
    -> Autoruns

14. What Sysinternals tool is used to investigate processes?
    -> ProcExp

15. What Sysinternals tool can be used to investigate network connection attempts?
    -> tcpVIEW

16. What Sysinternals tool can view permissions?
    -> Accesschk and AccessEnum

17. What Sysinternals tool allows us to view and modify handles?
    -> Handle

18. What is the default Windows user directory for files downloaded from the internet? (folder name only)
    -> downloads

19. What is the default Windows download directory that everyone has access to? (absolute path)
    -> c:\users\public\downloads

20. What Sysinternals tool shows service load order?
    -> LoadOrder

21. What is the service name of Windows Defender Firewall?
    -> MpsSvc

22. What SysInternals tool reports .dlls loaded into processes?
    -> ListDlls


--------------------
### Challenges (Workstation1)
--------------------

23. Malware named similarly to a legit executable, DLL in same folder. Flag = DLL name.
    -> Steps:
       1. Opened PowerShell as Administrator  
          Start → PowerShell → Run as admin
       2. Navigated suspicious dirs:  
          cd "C:\Users\Public"  
          cd "C:\Users\Public\Downloads"  
          dir  
          Found suspicious scvhost.exe (mimics svchost.exe)
       3. Listed DLLs:  
          dir *.dll  
          Found: libmingwex-0.dll
       4. Flag = libmingwex-0.dll

24. Annoying popup process → flag = exe name
    -> McAfeeFireTray.exe

25. Determine what is sending out SYN_SENT messages.
    -> McAfeeFireTray.exe (Sysinternals tcpVIEW)

26. Malware uses legit process names. Flag located in Kerberos registry subkey.
    -> Steps:
       1. Launch Autoruns → check "Services" tab
       2. Find Kerberos entry marked "Not Verified"
       3. Jump to registry location
       4. Inspect Parameters subkey for string values
       5. Extracted flag
       -> ✅ 76aGreX5

27. Malware named TotallyLegit. Find binary, read file in dir.
    -> GwlkK3sa

28. Find McAfeeFireTray.exe directory, read file inside.
    -> StrongBad

29. Permissions for NT SERVICE\TrustedInstaller on spoolsv.exe
    -> read and write

30. PATH from handle for spoolsv.exe
    -> C:\Windows\System32\en-US\spoolsv.exe.mui

31. In what Load Order Group is Windows Firewall service?
    -> NetworkProvider

32. First .dll associated with winlogon.exe (name only)
    -> ntd.dll

33. Windows Defender Firewall → LogAllowedConnections setting (Public profile)
    -> false

34. Nonstandard port opened by malware
    -> Steps:
       1. CMD as Administrator
       2. netstat -ano
       3. Check LISTENING ports, note PID
       4. tasklist /FI "PID eq 2412"
       -> Flag = port # (not yet filled)

35. Determine mechanism opening port from hidden_processes_7
    -> legit_script.ps1

36. Identify flag from file in hidden_processes_8
    -> Steps:
       1. Suspicious command:  
          powershell.exe -noexit -windowstyle hidden -File C:\windows\system32\legit_script.ps1
       2. Locate script:  
          C:\Windows\System32\legit_script.ps1
       3. Inspect contents (text editor or PowerShell ISE)
       4. Search for flag patterns (# FLAG:..., $flag = "...")
       5. Decode Base64 if needed:
          $encoded = "ZmxhZ3tzaGVsbF9leGVjdXRpb25fOD0hfQ=="
          [System.Text.Encoding]::UTF8.GetString([System.Convert]::FromBase64String($encoded))
       6. Final flag extracted:
          -> N0t_L3g1T_Ammiright


=========================
 10_WINDOWS UAC
=========================

1. What Sysinternals tool will allow you to view a file's manifest?  
    -> sigcheck  

2. What is the RequestedExecutionLevel for an application to run with the same permissions as the process that started it?  
    -> asInvoker  

3. What RequestedExecutionLevel will prompt the user for Administrator credentials if they're not a member of the Administrator's group?  
    -> requireAdministrator  

4. What registry key holds UAC values?  
    -> HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System  

5. The flag is the RequestedExecutionLevel of the schtasks.exe file.  
    -> asInvoker  

6. Determine which UAC subkey property shows whether UAC is enabled or not. The flag is the data value in that property.  
    -> Steps:  
        - Command:  
          (Get-ItemProperty -Path "HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System").EnableLUA  
        - Flag: 4919  

7. Provide the name of the UAC [Registry subkey] property that determines what level UAC is set to for admin privileges (Default, Always, Notify).  
    -> Steps:  
        - Command:  
          Get-ItemProperty -Path "HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System" -Name ConsentPromptBehaviorAdmin  
        - Flag: ConsentPromptBehaviorAdmin  

8. Query the registry subkey where UAC settings are stored, and provide the flag.  
    -> Steps:  
        - Command:  
          Get-ItemProperty -Path "HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System"  
        - Flag: NiceJob  


=========================
 11_WINDOWS SERVICES
=========================

1. What command-line (cmd) command will show service information?  
    -> sc query  

2. What command-line (cmd) command will show all services, running or not running?  
   The flag is the full command, for all services.  
    -> sc query state= all  

3. What PowerShell command will list all services?  
    -> Get-Service  

4. What registry location holds all service data?  
    -> HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Services  

5. What registry subkey holds a service's .dll location?  
   The flag is the subkey name (full path not needed).  
    -> Parameters  

6. Services have a name and display name, which could be different.  
   What is the service name of the only Totally-Legit service?  
   Reminder: All challenges are done using the file-server.  
    -> Steps:  
        - Run: Get-Service | Where-Object { $_.DisplayName -eq "Totally-Legit" }  
        - Flag: Legit  

7. Figure out the SID of the only Totally-Legit service.  
   Example: S-1-5-80-159957745-2084983471-2137709666-960844832-[4182961472]  
   Submit only the [bracketed] portion of the SID.  
   HINT: Run the command on the service name, not the display name.  
    -> Steps:  
        - Command:  
          (New-Object System.Security.Principal.NTAccount ("NT SERVICE\Legit")).Translate([System.Security.Principal.SecurityIdentifier]).Value  
        - Output:  
          S-1-5-80-159957745-2084983471-2137709666-960844832-1182961511  
        - Flag: 1182961511  
