Windows_PowerShell_Alias:
    -> 5

What PowerShell command lists all of the contents of a directory? 
    -> Get-childitem but it also can be Dir or ls since the question is very vauge. 

How to get the count of get-process properties in value: 
    -> (Get-Process | Select-Object -First 1 | Get-Member -MemberType Property).Count

how to get the count of words in a file: 
    -> (get-content .\words2.txt).count

how to get teh count for files inside the videos or an folder: 
    -> (Get-childitem "C:\Users\CTF\Videos").count

Compare files and output the difference:
    -> Compare-Object (Get-Content new.txt) (Get-content old.txt)

Sort and find specific line from a file:
    -> (get-content .\words.txt | Sort-Object -Descending)[20]

Count the number of unique words in words.txt: 
    -> (Get-Content words.txt | Sort-Object -Unique | Measure-Object).count 

How many methods are available for the get-process cmdlet?
Machine: Admin_Station
    -> (Get-Process | Get-Member -MemberType Method).count

Count the number of times, case-insensitive, gaab is listed in words.txt **Note: File Location - C:\Users\CTF\Desktop\CTF:
    -> (Select-String -Path words.txt -Pattern "gaab").count

Count the number of words, case-insensitive, with either a or z in a word, in the words.txt file:
    -> (Select-String -Path words.txt -Pattern "[az]").count

Use a PowerShell loop to unzip the Omega file 1,000 times and read what is inside.# Note: Make sure you back up the .zip file to a different directory before attempting this challenge
    -> Unzips each layer into its own folder, Stops when no more zip files are found
	-> Step 1: # Setup
$startZip = "Omega1000.zip"
$workingDir = "Omega1000.Unzipped"
$layer = 1

# Create working directory
if (-not (Test-Path $workingDir)) {
    New-Item -ItemType Directory -Path $workingDir | Out-Null
}

# Copy initial zip
Copy-Item $startZip "$workingDir\Layer_$layer.zip"

# Loop through nested zips
while ($true) {
    $zipPath = "$workingDir\Layer_$layer.zip"
    $extractPath = "$workingDir\Layer_$layer"

    Expand-Archive -Path $zipPath -DestinationPath $extractPath -Force
    Write-Host "📂 Unzipped Layer $layer"

    # Look for next zip
    $nextZip = Get-ChildItem -Path $extractPath -Filter *.zip -Recurse | Select-Object -First 1

    if ($nextZip) {
        $layer++
        Copy-Item $nextZip.FullName "$workingDir\Layer_$layer.zip"
    } else {
        Write-Host "✅ Final layer reached at Layer $layer"
        break
    }
}

# Read contents of final layer
$finalFiles = Get-ChildItem -Path $extractPath -File -Recurse

foreach ($file in $finalFiles) {
    Write-Host "`n📖 Reading file: $($file.Name)"
    Get-Content $file.FullName
}
	- Step 2: Answer is Kung-Fu
###############################################################################################################

Count the number of words in words.txt that meet the following criteria:
a appears at least twice consecutively and is followed immediately by any of the letters a through g
Note: File Location - C:\Users\CTF\Desktop\CTF
Example: aac...aaa...
    -> (Select-String -Path words.txt -Pattern "a{2,}[a-g]").Count

What command would tell you if there was a profile loaded for All Users All Hosts?
Flag is the full command syntax
    -> Test-Path $PROFILE.AllUsersAllHosts

Challenge only allows ONE attempt
Malware is running on a PowerShell profile on the File-Server. Based on PowerShell profile order of precedence (what is read first), find the correct flag.
The flag is the string after the #, without the preceding space.
    -Step 1: $Profile
    -Step 2: Get-Content $Profile --->>> output of this should be your flag. 

To find a user by using SID:
    -> Get-localuser | select name, sid

Script to connect to file server:
    -> New-PSDrive -Name FileServer -PSProvider FileSystem -Root "\\file-server\warrior share"

Why do we care?
We are looking for keys that can be used for Persistence
    Persistence consists of techniques that adversaries use to keep access to systems across restarts, changed credentials, and other interruptions that could cut off their access

As well as Privilege Escalation.
    Privilege Escalation consists of techniques that adversaries use to gain higher-level permissions on a system or network.


________________________________________________________________________________________________________

Registry Capture the flags

What Windows registry path is the Volatile Hive?
	-> HKLM\HARDWARE

What registry key creates the Wow6432Node to represent 32-bit applications that run on a 64-bit version of Windows?
	-> HKEY_LOCAL_MACHINE\SOFTWARE

In what registry path are the BOOT_START drivers located?
	-> HKLM\SYSTEM\CurrentControlSet\Services

What start value do BOOT_START drivers have in the registry?
	-> 0x0

During kernel initialization, what registry location is read containing all SYSTEM_START drivers?
	-> HKLM\SYSTEM\CurrentControlSet\Services

SERVICE_AUTO_START drivers and services are loaded after kernel initialization. What start value do they have in the registry?
	-> 0x2

What start value do SERVICE_DEMAND_START drivers and services have in the registry?
	-> 0x3

When accessing a remote registry which are the only 2 accessible HKEYs?
	-> HKLM and HKU

What PowerShell cmdlet will list currently mapped drives?
	-> Get-PSDrive

What is the native Windows GUI tool for managing the registry?
	-> Regedit

What registry hive contains all machine settings?
	-> HKEY_LOCAL_MACHINE (HKLM)

What registry hive contains all user settings?
	-> HKEY_USERS (HKU)

What registry hive contains only the currently logged-in user's settings?
	-> HKEY_CURRENT_USERwhoami

The HKEY_CURRENT_USER registry hive is a symbolic link to another registry subkey. What is the subkey that it is linked to?
	-> Step 1: whoami \user to get the SID
	-> Step 2: reg query "HKEY_USERS\S-1-5-21-2881336348-3190591231-4063445930-1004" to verify the 			   SID.

What PowerShell command will list all the subkeys and contents in the current directory and/or will list all the subkeys and the contents of a directory you specify?
	-> Get-ChildItem

What PowerShell command will list only the contents of a registry key or subkey?
	-> Get-Item

What registry subkey runs every time the machine reboots? The flag is the full path, using PowerShell.
Flag format: FULL\PATH\ALL\CAPS 
	-> HKLM\SOFTWARE\MICROSOFT\WINDOWS\CURRENTVERSION\RUN

What registry subkey runs every time a user logs on? The flag is the full path, using PowerShell.
Flag format: FULL\PATH\ALL\CAPS
	-> HKCU\SOFTWARE\MICROSOFT\WINDOWS\CURRENTVERSION\RUN

9. What registry subkey runs a single time, then deletes its value once the machine reboots? The flag is the full path, using PowerShell.
Flag format: FULL\PATH\ALL\CAPS
	-> HKLM\SOFTWARE\MICROSOFT\WINDOWS\CURRENTVERSION\RUNONCE

10. What registry subkey runs a single time, then deletes its value when a user logs on? The flag is the full path, using PowerShell.
Flag format: FULL\PATH\ALL\CAPS
	-> HKCU\SOFTWARE\MICROSOFT\WINDOWS\CURRENTVERSION\RUNONCE

11. What is the suspicious value inside of the registry subkey from your previous challenge named registry_basics_7?
	-> Step 1: reg query HKLM\SOFTWARE\Microsoft\Windows\CurrentVersion\Run 
	-> Step 2: Answer is C:\malware.exe

12. What is the suspicious value inside of the registry subkey that loads every time the "Student" user logs on?
	-> Step 1: get-localuser | select name, sid 
	-> Step 2: Reg query HKEY_USERS\S-1-5-21-2881336348-3190591231-4063445930
1003\Software\Microsoft\Windows\CurrentVersion\Run 

13. What is the value inside of the registry subkey from registry_basics_9?
	-> Step 1: reg query "HKLM\SOFTWARE\MICROSOFT\WINDOWS\CURRENTVERSION\RUNONCE".ToUpper()
	-> Step 2: Answer is C:\Virus.exe

14. What is the value inside of the registry subkey that loads a single time when the "student" user logs on?
	-> Step 1: reg query HKEY_USERS\S-1-5-21-2881336348-3190591231-4063445930-1003\Software\Microsoft\Windows\CurrentVersion\RunOnce
	-> Step 2: Answer C:\worm.exe

15. Figure out the manufacturer's name of the only USB drive that was plugged into this machine.
	-> Step 1: reg query "HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Enum\USBSTOR" 
	-> Step 2: outpput HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Enum\USBSTOR
    SanDisk9834    REG_SZ    bad
	- Step 3: Answer is SanDisk

16. What suspicious user profile, found in the registry, has connected to this machine?
	-> Step 1: Run this script: Get-ChildItem "HKLM:\SOFTWARE\Microsoft\Windows NT\CurrentVersion\ProfileList" |
ForEach-Object {
    $sid = $_.PSChildName
    $profilePath = (Get-ItemProperty $_.PSPath).ProfileImagePath
    [PSCustomObject]@{
        SID = $sid
        ProfilePath = $profilePath
    }
}
	-> Step 2: Profile path List scan and find the profile and answer is Hacker_McHackerson


17. What suspicious wireless network, found in the registry, has this system connected to?
	-> step 1: reg query "HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows NT\CurrentVersion\NetworkList\Profiles" 
	-> step 2: once step 1 is done run this script -> Get-ChildItem "HKLM:\SOFTWARE\Microsoft\Windows NT\CurrentVersion\NetworkList\Profiles" |
ForEach-Object {
    $profile = Get-ItemProperty $_.PSPath
    [PSCustomObject]@{
        GUID = $_.PSChildName
        SSID = $profile.ProfileName
        LastConnected = $profile.DateLastConnected
    }
}
	-> Step 3: is the anser case on the SID NAME: Terror_cafe_network


______________________________________________________________________________________________________________________

# Windows Alternate Data Stream
1. What is the smallest addressable unit on a hard disk?
	-> 
______________________________________________________________________________________________________________________
# Windows Boots Process
1. What is the smallest addressable unit on a hard disk?
	-> Fiel System Format
2. What are NTFS partition sectors grouped into?
	-> cluster or clusters
3. What contains the metadata about all of the files and directories on a NTFS partition?
	-> master file table (MFT) 
4. NTFS files are collections of what?
	-> 

















